akshare:
  enabled: true
  proxy_mode: env
  request_interval_ms: 1000
  timeout_seconds: 30
  max_retries: 3
  retry_backoff: exponential
  batch_size: 1
  max_concurrency: 1

yfinance:
  enabled: true
  proxy_mode: env
  request_interval_ms: 500
  timeout_seconds: 15
  max_retries: 2
  retry_backoff: exponential
  batch_size: 20
  max_concurrency: 2

baostock:
  enabled: true
  proxy_mode: env
  request_interval_ms: 800
  timeout_seconds: 20
  max_retries: 2
  retry_backoff: exponential
  batch_size: 1
  max_concurrency: 1

openbb:
  enabled: false
  proxy_mode: env
  request_interval_ms: 1000
  timeout_seconds: 30
  max_retries: 2
  retry_backoff: exponential
  batch_size: 10
  max_concurrency: 1

efinance:
  enabled: true
  proxy_mode: env
  request_interval_ms: 800
  timeout_seconds: 20
  max_retries: 2
  retry_backoff: exponential
  batch_size: 1
  max_concurrency: 1

tushare:
  enabled: false
  proxy_mode: env
  request_interval_ms: 800
  timeout_seconds: 20
  max_retries: 2
  retry_backoff: exponential
  batch_size: 1
  max_concurrency: 1
